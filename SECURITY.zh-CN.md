# 安全政策

## 报告问题

如果发现安全问题，请优先通过 GitHub security advisories 报告；如果不可用，可以创建不包含利用细节的最小 issue。

## 范围

`runbook-lint` 只解析本地 Markdown 并写报告。它不应该执行 runbook 命令，也不应该连接基础设施。导致命令执行、在指定输出路径之外写文件，或产生误导性安全结果的问题都属于范围。

## 支持版本

当前 `0.x` 版本线会接收安全修复。
